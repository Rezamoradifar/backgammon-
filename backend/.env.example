# Copy to .env for local development. Never commit .env itself.

DATABASE_URL="postgresql://backgammon:backgammon_dev_password@localhost:5432/backgammon_dev"

# Chain
CHAIN_ID=97
RPC_URL=
GAME_MANAGER_ADDRESS=
PLAYER_REGISTRY_ADDRESS=

# Only set while GameManager's active randomness provider is
# MockRandomnessProvider (testnet/dev only - never point this at a mainnet
# deployment). Without these two, RandomnessRequested events are ignored and
# startGame() will never progress past AWAITING randomness, since nothing
# else ever calls fulfill(). See gameManagerIndexer.ts's onRandomnessRequested.
MOCK_RANDOMNESS_PROVIDER_ADDRESS=
MOCK_RANDOMNESS_RELAYER_KEY=

# Private key of the wallet holding GameManager's REWARD_DISTRIBUTOR_ROLE.
# Without it, the weekly top-wagerer reward job does not run (logged clearly,
# not a silent no-op). This wallet only ever needs enough BNB for gas - it
# never holds player funds itself, see distributeWeeklyRewards' NatSpec.
WEEKLY_REWARD_DISTRIBUTOR_KEY=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Cosmetics shop (dice/board skins). Purchases are real on-chain payments
# (native BNB, or the USDT token below) that the backend verifies directly
# against the chain before granting the item - see routes/shop.ts. Treasury
# defaults to OWNER_FEE_WALLET if unset, since that's already an
# owner-controlled wallet with no reason to duplicate.
SHOP_TREASURY_ADDRESS=
OWNER_FEE_WALLET=
# The deployed stake token also used for USDT-priced shop purchases (see
# DEPLOYMENT.md's MockUSDT address on testnet).
USDT_TOKEN_ADDRESS=

PORT=4000
